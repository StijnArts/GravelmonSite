{
  "name": "dynamodb-graph-layer-nodejs",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js runtime contents for the DynamoDB graph Lambda layer.",
  "scripts": {
    "build": "if exist dist rmdir /s /q dist && tsc && node scripts/build-layer.js",
    "bootstrap": "npm install && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.358.0",
    "@aws-sdk/util-dynamodb": "^3.358.0"
  }
}
