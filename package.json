{
  "name": "aws-dynamodb-sam",
  "version": "1.0.0",
  "description": "AWS SAM Node.js app using DynamoDB",
  "main": "dist/handler.js",
  "scripts": {
    "setup-layer": "node scripts/setup-layer.js",
    "start": "npm run setup-layer && sam local start-api --template template.yaml --port 3000",
    "build": "tsc",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.358.0",
    "@aws-sdk/util-dynamodb": "^3.358.0",
    "aws-cdk-lib": "^2.252.0",
    "constructs": "^10.6.0",
    "gravelmon-dynamodb": "github:Gravelmon/GravelmonWebApp-DynamoLayer#main",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@sinonjs/fake-timers": "^10.3.0",
    "@types/aws-lambda": "^8.10.100",
    "@types/jest": "^30.0.0",
    "@types/node": "^18.0.0",
    "@types/prettier": "2.6.0",
    "aws-cdk": "^2.28.1",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "0.25.0",
    "jest": "^30.3.0",
    "typescript": "^5.0.0",
    "dotenv": "^17.4.2"
  }
}
