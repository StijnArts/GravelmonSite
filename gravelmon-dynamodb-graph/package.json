{
  "name": "gravelmon-dynamodb-graph",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone project for gravelmon DynamoDB graph utilities.",
  "scripts": {
    "bootstrap": "npm install --prefix nodejs && npm run bootstrap --prefix nodejs"
  }
}
